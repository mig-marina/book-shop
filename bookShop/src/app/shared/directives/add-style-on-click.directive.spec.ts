import { AddStyleOnClickDirective } from './add-style-on-click.directive';

describe('AddStyleOnClickDirective', () => {
  it('should create an instance', () => {
    const directive = new AddStyleOnClickDirective();
    expect(directive).toBeTruthy();
  });
});
