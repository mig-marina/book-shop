import { CartItemOnHoverDirective } from './cart-item-on-hover.directive';

describe('CartItemOnHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new CartItemOnHoverDirective();
    expect(directive).toBeTruthy();
  });
});
